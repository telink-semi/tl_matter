# TL7218X standard RKE using the existing single-advertiser controller.
# Use instead of prj_ble.conf. Matter commissioning takes advertising priority;
# Aliro advertises after commissioning closes, while Matter runs over Thread.
CONFIG_ALIRO_TRANSPORT_NFC=n
CONFIG_ALIRO_TRANSPORT_BLE=y
CONFIG_ALIRO_FAST_TRANSACTION=n
CONFIG_ALIRO_BT_POLLER_THREAD_STACK_SIZE=4096

CONFIG_BT_MAX_CONN=1
CONFIG_TL_BLE_CTRL_SLAVE_MAX_NUM=1
CONFIG_BT_ID_MAX=2
CONFIG_BT_BONDABLE=n
CONFIG_BT_EXT_ADV=n
CONFIG_BT_L2CAP_DYNAMIC_CHANNEL=y
CONFIG_BT_RX_STACK_SIZE=3072

# BLE and Thread still share the radio, even with a single BLE advertiser.
CONFIG_IEEE802154_TLX_BLE_COEXIST=y
CONFIG_DYNAMIC_INTERRUPTS=y
CONFIG_TL_BLE_CTRL_THREAD_STACK_SIZE=8192
